{
    "client": {
        "name": "<%= node.fqdn %>",
        "address": "<%= node.ipaddress %>",
        "subscriptions": <%= node.roles %>,

        "graphite": {
            "name": "<%= node.fqdn.gsub('.', '_') %>"
        },

        "disk": {
            "wspace": 80,
            "cspace": 90,
            "winode": 80,
            "cinode": 90,
            "mount": "/$,/mnt$,/data"
        }
    }
}
